<%= form_for(@local_pedido) do |f| %>
  <% if @local_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@local_pedido.errors.count, "error") %> prohibited this local_pedido from being saved:</h2>

      <ul>
      <% @local_pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :local_id %>:
    <%= f.select :local_id, Local.all.map{|t| [t.loc_rs,t.id]}, {}, {}  %>
  </div>

  <div class="field">
    <%= f.label :estado_id %>:
    <%= f.select :estado_id, Estado.all.map{|t| [t.est_descripcion,t.id]}, {}, {}  %>
  </div>

  <div class="field">
    <%= f.label :pedido_id %>:
    <%= f.select :pedido_id, Pedido.all.map{|t| [t.id,t.id]}, {}, {}  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
