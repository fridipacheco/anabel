<%= form_for(@seccion) do |f| %>
  <% if @seccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seccion.errors.count, "error") %> prohibited this seccion from being saved:</h2>

      <ul>
      <% @seccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sec_seccion %><br>
    <%= f.text_field :sec_seccion %>
  </div>
  <div class="field">
    <%= f.label :sec_descripcion %><br>
    <%= f.text_field :sec_descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
