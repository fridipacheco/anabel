<h1>Listing tipo_turnos</h1>

<table>
  <thead>
    <tr>
      <th>Ttu descripcio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_turnos.each do |tipo_turno| %>
      <tr>
        <td><%= tipo_turno.ttu_descripcio %></td>
        <td><%= link_to 'Show', tipo_turno %></td>
        <td><%= link_to 'Edit', edit_tipo_turno_path(tipo_turno) %></td>
        <td><%= link_to 'Destroy', tipo_turno, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo turno', new_tipo_turno_path %>
