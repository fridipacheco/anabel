<%= form_for(@producto) do |f| %>
  <% if @producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% @producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pro_nombre %><br>
    <%= f.text_field :pro_nombre %>
  </div>
  <div class="field">
    <%= f.label :pro_descripcion %><br>
    <%= f.text_field :pro_descripcion %>
  </div>
  <div class="field">
    <%= f.label :tipo_producto_id %>:
    <%= f.select :tipo_producto_id, TipoProducto.all.map{|t| [t.tip_descripcion,t.id]}, {}, {}  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
