<%= form_for(@vehiculo) do |f| %>
  <% if @vehiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehiculo.errors.count, "error") %> prohibited this vehiculo from being saved:</h2>

      <ul>
      <% @vehiculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :veh_patente %><br>
    <%= f.text_field :veh_patente %>
  </div>
  <div class="field">
    <%= f.label :veh_capacidad %><br>
    <%= f.text_field :veh_capacidad %>
  </div>
    <div class="field">
      <%= f.label :modelo_id %><br>
      <%= f.select :modelo_id, Modelo.all.map{|t| [t.mod_descripcion,t.id]}, {}, {}  %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
