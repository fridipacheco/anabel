<%= form_for(@turno) do |f| %>
  <% if @turno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@turno.errors.count, "error") %> prohibited this turno from being saved:</h2>

      <ul>
      <% @turno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tur_horario %><br>
    <%= f.text_field :tur_horario %>
  </div>
  <div class="field">
    <%= f.label :tipo_turno_id %><br>
    <%= f.select :tipo_turno_id, TipoTurno.all.map{|t| [t.ttu_descripcio,t.id]}, {}, {}  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
