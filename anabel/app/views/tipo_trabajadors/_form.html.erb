<%= form_for(@tipo_trabajador) do |f| %>
  <% if @tipo_trabajador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_trabajador.errors.count, "error") %> prohibited this tipo_trabajador from being saved:</h2>

      <ul>
      <% @tipo_trabajador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tpt_descripcion %><br>
    <%= f.text_field :tpt_descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
