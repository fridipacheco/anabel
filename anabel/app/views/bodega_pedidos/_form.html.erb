<%= form_for(@bodega_pedido) do |f| %>
  <% if @bodega_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bodega_pedido.errors.count, "error") %> prohibited this bodega_pedido from being saved:</h2>

      <ul>
      <% @bodega_pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bodega_id %>:
    <%= f.select :bodega_id, Bodega.all.map{|t| [t.bod_rs,t.id]}, {}, {}  %>
  </div>
  <div class="field">
    <%= f.label :estado_id %>:
    <%= f.select :estado_id, Estado.all.map{|t| [t.est_descripcion,t.id]}, {}, {}  %>
  </div>
  <div class="field">
    <%= f.label :pedido_id %>:
    <%= f.select :pedido_id, Pedido.all.map{|t| [t.id,t.id]}, {}, {}  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
